apiVersion: v1
kind: ResourceQuota
metadata:
  name: todo-quota
  namespace: todo
spec:
  hard:
    requests.memory: "750Mi"
    requests.cpu: "600m"
    limits.memory: "1200Mi"
    limits.cpu: "1200m"
    pods: "9"

