apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: todo-dev

resources:
  - ../../base
  - resourcequota.yaml
  - namespace.yaml
  - sealed-todo-secrets.yaml

commonLabels:
  environment: development

# 이미지 태그 설정
images:
  - name: ghcr.io/mayne0213/todo
    newTag: develop-sha-a76b98071375ac934b8cd039e2d6b2ed1354e278

patchesStrategicMerge:
  - deployment-patch.yaml
